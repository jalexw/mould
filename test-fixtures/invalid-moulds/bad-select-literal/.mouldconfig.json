{ "inputs": [ { "id": "mode", "label": "Mode", "required": false, "type": "select", "options": ["a", "b"], "default": "a" } ], "conditionalPaths": [ { "when": "mode == c", "paths": ["file.txt"] } ] }
