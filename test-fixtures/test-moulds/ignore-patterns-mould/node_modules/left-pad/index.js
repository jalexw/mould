// Installed dependency: must NOT be copied into the scaffolded app
module.exports = function leftPad(str, len, ch) {
  str = String(str);
  ch = ch === undefined ? " " : String(ch);
  while (str.length < len) str = ch + str;
  return str;
};
