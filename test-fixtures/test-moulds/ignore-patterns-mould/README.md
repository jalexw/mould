# XxX_AppName_XxX

A small app used to verify that `ignorePatterns` in `.mouldconfig.json` keeps
build output (`dist/`), installed dependencies (`node_modules/`) and log files
(`*.log`) out of the scaffolded project.
